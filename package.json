{
  "name": "ai-dlc-crud-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-role end-to-end tests for the ai-dlc-crud stack. Backend + frontend are in their own packages; this root only ships Playwright config and specs.",
  "scripts": {
    "e2e:install": "playwright install chromium",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
